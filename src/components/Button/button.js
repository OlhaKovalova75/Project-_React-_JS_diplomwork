import "./index.css";
const Button = () => {
  // const Button = (props) => {
  // const input = <input className="input" type={props.inputType} />;
  // return <div>{input}</div>;

  return (
    <div>
      <button className="button">Preview</button>
    </div>
  );
};

export default Button;
